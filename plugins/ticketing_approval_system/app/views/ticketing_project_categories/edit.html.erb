<h2>Edit Category</h2>
<%= form_for @category, :as => :issue_category,
             :url => project_ticketing_project_category(@project, ProjectCategory.first), :method => 'put' do |f| %>
    <%= render :partial => 'ticketing_project_categories/form' %>
    <%= submit_tag 'Update' %>
<% end %>

