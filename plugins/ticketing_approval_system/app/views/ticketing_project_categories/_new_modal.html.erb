<h3 class="title"><%=l(:label_issue_category_new)%></h3>
<%= form_for @category, :as => 'ticketing_project_categories', :url => project_ticketing_project_categories_path(@project), :remote => true do |f| %>
<%= render :partial => 'ticketing_project_categories/form', :locals => { :f => f } %>
  <p class="buttons">
    <%= submit_tag l(:button_create), :name => nil %>
    <%= submit_tag l(:button_cancel), :name => nil, :onclick => "hideModal(this);", :type => 'button' %>
  </p>
<% end %>
